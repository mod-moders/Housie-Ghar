# ── Dependencies ──
node_modules/
.pnp
.pnp.js

# ── Env & secrets ──
.env
.env.*
!.env.example
*.pem
private.pem
public.pem
*.key

# ── Build output ──
.next/
out/
dist/
build/
*.tsbuildinfo
next-env.d.ts

# ── Logs ──
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Caches / tooling ──
.turbo/
.cache/
coverage/
.eslintcache

# ── Backups & data dumps ──
HG/backups/
*.sql.gz
*.dump

# ── OS / editor ──
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# ── Claude Code local config ──
.claude/

# ── Large binaries (should never be in the repo) ──
*.dmg
*.pkg
*.zip
*.crdownload

# ── Design source images & local screenshots (not needed by the app) ──
# Real app assets live in HG/frontend/public/ and ARE committed; these
# root-level originals and macOS screenshots are local-only.
/HG Logo *.png
Screenshot*.png
._*

# ── Unrelated project ──
ruflo/

.superpowers/

# Compiled shared types (build artifacts)
HG/shared/types/*.js
HG/shared/types/*.js.map
HG/shared/types/*.d.ts
HG/shared/types/*.d.ts.map
