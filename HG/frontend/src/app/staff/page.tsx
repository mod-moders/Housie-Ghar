"use client";
/** Generic staff dashboard (unlisted Operator + safety-net entry). */

import { StaffShell } from "@/components/staff/StaffShell";

export default function StaffDashboard() {
  return <StaffShell />;
}
